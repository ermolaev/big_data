plugins {
  id 'java'
  id 'maven'
  id 'scala'
  id 'com.github.johnrengelman.shadow' version '1.2.2'
}

group = 'ermolaev'
version = '0.1-SNAPSHOT'

description = """Kafka Spark Streaming"""

shadowJar {
  zip64 true
}

repositories {
     maven { url "https://repository.apache.org/content/repositories/releases/" }
     maven { url "http://scala-tools.org/repo-releases" }
     maven { url "http://repo.maven.apache.org/maven2" }
}
dependencies {
    compile group: 'com.github.nscala-time', name: 'nscala-time_2.10', version:'2.0.0'
    compile group: 'mysql', name: 'mysql-connector-java', version:'5.1.28'
    compile group: 'org.scalaz', name: 'scalaz-core_2.10', version:'7.1.3'
    compile group: 'org.scalatest', name: 'scalatest_2.10', version:'2.2.4'
    compile(group: 'org.apache.spark', name: 'spark-streaming-kafka_2.10', version:'1.5.2') {
exclude(module: 'slf4j-log4j12')
    }
    compile group: 'org.scalanlp', name: 'breeze_2.10', version:'0.9'
    compile group: 'com.datastax.spark', name: 'spark-cassandra-connector_2.10', version:'1.3.0'
    compile group: 'org.apache.spark', name: 'spark-hive_2.10', version:'1.3.0'
    compile group: 'com.datastax.cassandra', name: 'cassandra-driver-core', version:'2.1.4'
    testCompile group: 'com.holdenkarau', name: 'spark-testing-base_2.10', version:'1.4.1_0.1.1'
    testCompile group: 'org.scalamock', name: 'scalamock-scalatest-support_2.10', version:'3.2'
    compile(group: 'org.apache.spark', name: 'spark-core_2.10', version:'1.5.2') {
exclude(module: 'slf4j-log4j12')
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.apache.spark', name: 'spark-streaming_2.10', version:'1.5.2') {
exclude(module: 'slf4j-log4j12')
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.apache.hadoop', name: 'hadoop-common', version:'2.6.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.slf4j', name: 'slf4j-api', version:'1.7.5') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.scala-lang', name: 'scala-library', version:'2.10.4') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'com.twitter', name: 'parquet-hadoop', version:'1.6.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'com.twitter', name: 'parquet-hive-bundle', version:'1.6.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.apache.spark', name: 'spark-sql_2.10', version:'1.5.2') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
